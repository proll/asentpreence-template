{.repeated section items}
  {.folder?}
    {.repeated section items}
      {.collection?}
        <section class="home__section home__section1">
          <a href="{collection.fullUrl}" class="home__section-h"><h1>{collection.pageTitle}</h1></a>
          <a href="{collection.fullUrl}" class="home__section-a"><span>View {collection.navigationTitle}</span></a>
        </section>
      {.end}
    {.end}
  {.end}
{.end}